# OpenCode Config

This repository contains my OpenCode configuration.

Its primary purpose is to keep my OpenCode setup consistent across multiple
machines. Most of the content is personal configuration and agent definitions.


## Usage

Simply clone this repository in your `.config` directory:

```sh
git clone git@github.com:mmohaveri/opencode.config.git ~/.config/opencode
```

